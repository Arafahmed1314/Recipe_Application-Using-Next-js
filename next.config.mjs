/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Add Unsplash as a valid image source
    },
};

export default nextConfig;
